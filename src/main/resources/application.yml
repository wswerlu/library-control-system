---
spring:
  output:
    ansi:
      enabled: always
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:library_control_system
    driverClassName: org.h2.Driver
    username: test
    password: test
